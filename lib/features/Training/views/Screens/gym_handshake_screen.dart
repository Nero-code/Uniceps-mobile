import 'package:flutter/material.dart';

@Deprecated("Gym Hand-shakes feature is disabled in version 1.0.0")
class GymHandShakeScreen extends StatelessWidget {
  const GymHandShakeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("GymHandshake"),
      ),
    );
  }
}
