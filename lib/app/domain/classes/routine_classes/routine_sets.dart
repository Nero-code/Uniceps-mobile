class RoutineSets {
  const RoutineSets({
    required this.id,
    required this.index,
    required this.reps,
    required this.weight,
  });

  final int? id;
  final int index, reps;
  final double? weight;
}
