class RoutineSets {
  const RoutineSets({
    required this.id,
    required this.apiId,
    required this.index,
    required this.reps,
    required this.weight,
  });

  final int? id, apiId;
  final int index, reps;
  final double? weight;
}
