import 'package:flutter/material.dart';

class SessionReportCard extends StatelessWidget {
  const SessionReportCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
